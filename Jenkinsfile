
pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                githttps://github.com/nouhasaidanee/student-management/edit/main/Jenkinsfile.git'
            }
        }

        stage('Build') {
            steps {
                sh 'mvn clean compile'
            }
        }
    }
}
